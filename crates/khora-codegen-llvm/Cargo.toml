[package]
name = "khora-codegen-llvm"
description = "Native backend: HIR -> LLVM IR via inkwell, linked with lld"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
# Off by default: enabling it requires a matching LLVM installation.
default = []
llvm = []

[dependencies]
khora-hir.workspace = true
khora-perceus.workspace = true
