# The workspace this repository is.
#
# **No `[package]`, because the root is not one.** `std`, four examples, four
# benchmarks and a library live under it, and none of them is here. A root
# forced to declare a package would be inventing a name for a thing that does
# not exist, and that name would then turn up in error messages. Roadmap 14.13.
#
# What this buys, concretely: `khora check .` at the root checks every member,
# each as its own package with its own dependencies. `scripts/baseline.sh` used
# to do that with a `for` loop and a comment explaining why the loop was
# necessary.
#
# `std` is not a member. It is found beside the compiler rather than resolved
# through a manifest -- `khora_db::standard_library` -- and it has no
# `khora.toml` at all, which is the same reason `khora check std` is still its
# own line in the baseline.

[workspace]
members = ["examples/*", "packages/*", "bench/*"]

# What every member takes with `field.workspace = true`. Roadmap 14.14.
#
# Nothing is inherited implicitly: a member that wants the shared version says
# so. The alternative -- a root value that applies unless overridden -- means
# reading a member's manifest tells you what it *adds*, and the only way to
# know what it *is* is to read two files and remember which wins.
[workspace.package]
version = "0.1.0"
edition = "2026"

# The formatter, in one place.
#
# This block was copied into three of the four examples verbatim, which is the
# argument for the feature in miniature: three copies of a decision that was
# only ever made once, and nothing to notice when one of them drifts.
[workspace.fmt]
indent-style = "space"
indent-width = 2

# A cap on what any member may ask for. Roadmap 14.19.
#
# **Not the same table as `[workspace.permissions]`.** That one is a default a
# member opts into with `workspace = true`; this is a cap a member cannot opt
# out of. Only the three examples that answer on a port may reach the network,
# and a fourth one that decides to is a build failure rather than something a
# reviewer has to notice.
#
# `extern = []` is the strict one and the one that matters most: nothing here
# may declare an `extern fn` or hand that permission to a dependency. `std`
# always may, which is the design rather than an exception to it -- everything
# leaving Khora goes through functions whose signatures carry capability rows,
# and those live in `std`. `docs/design/permissions.md`.
#
# The first half of that was not enforced until recently. `check_extern_allowlist`
# walked the resolved *dependencies*, so a member could write `extern = []` in
# its own manifest and declare an `extern fn` on the next screen. The package
# being built is checked against its own list now, which is what the rule always
# said: `may_declare_extern` is about which package a declaration is in, and a
# package is no less itself for being the one at the root of the build.
[workspace.policy]
network = ["ledger_service", "link_shortener", "risk_analyzer"]
env = ["ledger_service", "link_shortener", "risk_analyzer"]
fs = ["khq", "link_shortener", "risk_analyzer"]
extern = []

# The two lints that are off by default, on for this repository.
#
# `khora_lint::default_level` warns for everything except these, and each has a
# reason that is about the language's users rather than about this checkout.
# Neither reason survives contact with the project's own standard.
#
# **`undocumented-export` is a `deny` because the gate had a hole exactly the
# shape of it.** `khora doc` regenerates the reference from `///` comments, and
# `scripts/baseline.sh` fails when a page is stale -- so a documented export
# cannot drift. Nothing checked that an export was documented at *all*: a new
# `pub fn` with no comment above it generated a page entry with nothing under
# it, passed every step, and shipped. `deny` rather than `warn` because a warn
# does not fail `khora check`, and a rule the gate does not enforce is a
# preference rather than a standard.
#
# It costs nothing today. `std`, four examples, two packages and four
# benchmarks were all clean when this was switched on, which is why it could be
# switched on at a stroke rather than as a project.
#
# **`useless-allow` stays a `warn`**, and the difference is deliberate. Its own
# doc comment argues it should be off while lints are still being added,
# because it fires on the lines somebody is already editing to satisfy a new
# one. That argument is about churn, and a warning does not fail the build --
# so what this buys is the yellow underline in the editor on a suppression that
# has outlived its reason, without the interruption of an error. A stale
# `allow` hides the next finding on that line, which is the thing worth being
# told about.
[lints]
undocumented-export = "deny"
useless-allow = "warn"

# What somebody runs before pushing. Roadmap 14.18.
#
# Run with `khora task ci`. Declared at the root, so it runs once here rather
# than eight times, one per member: `ci` in a monorepo means "run the pipeline",
# not "run something called ci in each package".
[tasks.ci]
description = "The full baseline: clippy, tests, the corpus, the conformance suite"
run = "bash scripts/baseline.sh"

[tasks.docs]
description = "Regenerate the standard library reference"
run = "khora doc"
