Curated standard-library concepts live here. Per-symbol pages are intended to be generated by `khora doc --stdlib` from the same source revision as the compiler release rather than maintained manually.
